// Generated by CoffeeScript 1.12.7
(function() {
  var Request;

  Request = (function() {
    function Request(requests) {
      this.requests = requests;
      $.ajaxSetup({
        cache: false
      });
      this.load_requests();
    }

    Request.prototype.load_requests = function() {
      var i, len, ref, request, results;
      ref = this.requests;
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        request = ref[i];
        console.log(request);
        results.push($('#req_list').tmpl({
          description: request.req_description,
          state: request.req_state
        }).appendTo(".listReq"));
      }
      return results;
    };

    return Request;

  })();

  window.Request = Request;

}).call(this);
